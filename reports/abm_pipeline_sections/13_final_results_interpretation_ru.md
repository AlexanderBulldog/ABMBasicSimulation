# Раздел 13. Интерпретация финального результата: что доказано и что еще не закрыто

Этот раздел переводит актуальные таблицы в научный вывод по состоянию на последние прогоны (`2026-02-14` и `2026-02-15`).

## 13.1 Короткий итог

На текущем срезе финальный строгий verdict = **FAIL**.

Причина: часть критичных blocking-гейтов не проходит одновременно в full-run и в 3-репликационной evidence-кампании.

---

## 13.2 Что показал полный adaptive run (`v6`)

Источник: `output_research_core_v6_full_20260214_182812`.

Ключевые факты:
- waves run: `5` (`stop_reason=max_waves_reached`)
- финальная волна: `wave_05`
- `NROY=68.40%` (выше active-вилки `[25,65]%`)
- `I_max median=2.4292` (PASS)
- `I_max p95=2.9273` (PASS)
- `Emulator CV-R2 median=0.5570` (FAIL)
- `confirmatory nroy_delta_pp=15.31` (FAIL)

Интерпретация:
- качество matching по implausibility хорошее,
- но фильтр по NROY слишком мягкий на финальной волне,
- устойчивость вывода между main и confirmatory недостаточна.

---

## 13.3 Что показала evidence-кампания v3

Источник: `output_evidence_campaign_v3_20260215/campaign_evidence_summary.json`.

### 13.3.1 Stage A

Все кандидаты `A1..A4` получили core-pass (с nroy-safe retry при необходимости).

Победитель: `A4`:
- `nroy_pct=0.30`
- `r2_median=0.6325`
- `imax_median=2.5874`
- quick-confirm: `|delta NROY|=1.67 п.п.` (проходит mini-confirm)

### 13.3.2 Stage B (репликации)

- `R1`: `nroy=59.43%`, `r2=0.6706`, но `confirm_delta=16.57 п.п.` + Employment floor FAIL
- `R2`: `nroy=44.57%`, `r2=0.6878`, но `confirm_delta=31.43 п.п.` + E/O/C floors FAIL
- `R3`: `nroy=35.71%`, `r2=0.6070`, но `confirm_delta=64.00 п.п.` + E/O/C floors FAIL

Итог Stage B:
- `pass_runs_count=0`
- acceptance `2-of-3 + consistency` не выполнена
- campaign status: `FAIL`

---

## 13.4 Что уже можно считать достижением

1. Ядро ABM стабильно держит structural блок:
- `PriceDispersion`, `InventoryGap`, `CreditRejectionRate mean` проходят в ключевых прогонах.

2. В Stage A удалось получить валидные core-pass кандидаты по нескольким конфигурациям.

3. NROY-safe retry (`2.55 -> 2.45`) работает как задумано и помогает возвращать NROY в активный диапазон.

4. Mini-confirm фильтр до Stage B уменьшил риск выбора заведомо нестабильного кандидата.

---

## 13.5 Что остается главным риском

1. Репликационная устойчивость confirmatory:
- большие `|nroy_delta_pp|` в независимых запусках.

2. Просадка representative floors в части репликаций:
- особенно employment/output/consumption.

3. Дрейф качества эмулятора между волнами:
- на финальной волне v6 `CV-R2 median` падает ниже порога.

---

## 13.6 Научная интерпретация текущего статуса

Корректная формулировка на сегодня:

- Модель и pipeline демонстрируют локально сильные режимы (включая успешные кандидаты Stage A),
- но итоговый строгий критерий воспроизводимости еще не закрыт,
- поэтому финальный научный вывод пока остается условно-позитивным, но формально `FAIL` по blocking-правилам.

---

## 13.7 Что это не означает

1. Это не означает "модель сломана".
2. Это означает "доказательная сила пока недостаточна для финального PASS".
3. При смене seed, волн и confirmatory-трассы часть выводов меняется сильнее допустимого.

---

## 13.8 Ключевая мысль раздела 13

Актуальный результат — это не финальная победа, а зрелая промежуточная точка:
- методология уже дает управляемые улучшения,
- но до формально устойчивого scientific PASS нужно закрыть репликационную стабильность и long-run floors в Stage B.
