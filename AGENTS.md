# AGENTS.md

## Цель репозитория (актуальный фокус)
- Текущая работа: анализ чувствительности ABM в парадигме **Bayesian History Matching + Emulation** (Andrianakis et al., PLOS Comp Bio, 2015).
- Цель подготовки среды: держать рабочее дерево чистым, чтобы можно было честно пересобирать LHS→ABM→эмулатор→HM→wave2.

## Что было сделано (история изменений среды)
- 2026-01-29: удалены все артефакты прогонов и архивы по запросу пользователя:
  - удалены каталоги: `output_seminar/` и `attic/` (а также любые `output/` / `output_backup/`, если бы существовали).
  - удалены Python-кэши в `src/` и `scripts/` (`__pycache__`, `*.pyc`).
- 2026-01-29: обновлён `.gitignore`, чтобы новые прогоны не попадали в git:
  - игнорируются `output/`, `output_seminar/`, `output_backup/`, `output_*`, `attic/`, а также `__pycache__/` и `*.pyc`.

## Где искать «источник истины» по методологии
- Документация семинарного набора и пайплайна (команды, шаги, артефакты): `reports/2026-01-25_seminar/`.
  - особенно: `reports/2026-01-25_seminar/02_pipeline_spec.md` и `reports/2026-01-25_seminar/07_reproduce.md`.

## Правила для будущих агентов
- Не хранить большие артефакты прогонов в репозитории — складывать их в `output_seminar/` или `output_<run_id>/` (они игнорируются git).
- Для честной оценки эмулятора избегать утечки по повторным `seed`: валидировать по группам уникальных `theta`.
- Любые изменения пайплайна (bounds/targets/пороги фильтрации/валидация) фиксировать в этом файле датой и кратким описанием.
